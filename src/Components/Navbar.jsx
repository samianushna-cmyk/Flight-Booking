import React from "react";

const Navbar = () => {
  return (
    <div className="navbar">
      <h2>Online Flight Booking System</h2>

      <div className="nav-buttons">
        <button>Help</button>
        <button>Login</button>
        <button>Sign Up</button>
      </div>
    </div>
  );
};

export default Navbar;
